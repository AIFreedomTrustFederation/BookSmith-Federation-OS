# Node Agent

Placeholder application for the future local sync/runtime agent.

The node agent should eventually coordinate local filesystem access, background jobs, sync, indexing, and federation node communication.

It exists now only to reserve the monorepo boundary.
